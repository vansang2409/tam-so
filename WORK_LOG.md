# WORK_LOG — Tam Sở (đã NÉN 19/06/2026)

> Log chi tiết cũ đã gom thành changelog gọn bên dưới. Chi tiết kiến trúc/gotcha: CLAUDE.md · bàn giao: SUMMARY.md · backlog: TASK_BOARD.md.

## Changelog theo version
- **v3.94 — Đi Chùa: thu nhỏ hộp "Đang ở" trên mobile, để lộ ảnh chùa** (chủ dự án: "phần dc-caption đang che hết người dùng ko thấy được á" — đúng, fix v3.90 hết đè chữ nhưng full-width+xuống dưới lại khiến nó CHIẾM gần hết khung ảnh: đo được caption cao 136px/300px khung — gần 93% khung ảnh bị 3 lớp overlay (biển hiệu+caption+nút điều khiển) che kín, gần như không còn khoảng trống thấy ảnh). Sửa ở `@media ≤760px`: ẩn `.dc-caption-desc` (đoạn mô tả dài, info phụ — tên địa điểm ở title đã đủ), giảm `.dc-caption-title` xuống 1.05rem, giảm padding, tăng `.dc-stage` min-height 260→300px. Kết quả đo lại: caption co từ 136px xuống 65px, lộ ra khoảng trống thấy ảnh rõ ~127px (trước đó gần như 0). 373 test.
- **v3.93 — Đi Chùa: sửa ĐÚNG chỗ thany cuộn thật** (chủ dự án: "check trên localhost vẫn bị nha... có thể là do scroll" — đúng, v3.91/3.92 sửa SAI chỗ): verify bằng `document.scrollingElement` → trả về `<html>`, KHÔNG phải `<body>` — toàn bộ CSS ẩn scrollbar + khoá overflow ở v3.91/3.92 nhắm vào `body.dc-lock` nên không chạm tới thanh cuộn thật của trang (luôn nằm ở `<html>` theo spec, dù style/overflow set trên body). Sửa: thêm `html:has(body.dc-lock) { overflow-x:hidden; scrollbar-width:none }` + ẩn `::-webkit-scrollbar` — verify lại bằng `getComputedStyle(document.documentElement)` xác nhận đúng giá trị khi đang ở Đi Chùa, và KHÔNG đụng tới html ở các trang sáng khác (đã so sánh 2 ngữ cảnh). 373 test.
- **v3.92 — Đi Chùa: khoá cuộn ngang ngoài ý muốn do ảnh nền mờ tràn viền** (chủ dự án gửi screenshot DevTools tiếp, "vẫn bị nha" sau v3.91 — nghi do Vercel/SW chưa kịp deploy bản vá trước, nhưng tìm thêm được lỗi thật khác): `.dc-page-bg` dùng `transform:scale(1.1)` (cố ý, để che viền cứng khi blur ảnh nền) nên tự nhiên tràn ra ngoài khung ~21px mỗi bên; `.dc-root` (desktop) và `body.dc-lock` (mobile, do `.dc-root` đổi `position:static` khiến `#root` thành ngữ cảnh định vị mới của `.dc-page-bg`) đều dùng `overflow:auto` (cả 2 chiều) nên phần tràn này tạo ra vùng cuộn NGANG không mong muốn (dù đã ẩn thanh cuộn ở v3.91, vùng cuộn vẫn tồn tại ngầm). Sửa: đổi cả hai sang `overflow-x:hidden; overflow-y:auto` — vẫn cuộn dọc bình thường, khoá hẳn khả năng cuộn/tràn ngang; verify desktop (1280px, cuộn dọc vẫn hoạt động) + mobile (~414px, không phần tử nào tràn quá khung body thật). 373 test.
- **v3.91 — Đi Chùa: ẩn thanh cuộn SÁNG chói trên nền TỐI** (chủ dự án gửi 2 screenshot tiếp, phản hồi "bên phải, footer bị dư rồi"): nguyên nhân là CSS scrollbar tuỳ biến toàn site (`::-webkit-scrollbar-track{background:ink2}` — màu SÁNG cho theme sáng toàn site) vẫn áp dụng cả lên trang Đi Chùa (theme TỐI riêng), tạo vệt sáng dọc bên phải (thanh cuộn dọc trang) và vệt sáng ngang dưới khay địa điểm (thanh cuộn ngang `.dc-slider`/`.dc-sidebar`) — chói hẳn trên nền navy. Lỗi chỉ lộ khi xem qua DevTools/Chrome desktop (scrollbar cổ điển luôn hiện), điện thoại thật dùng scrollbar overlay tự ẩn nên không thấy — nhưng vẫn là lỗi thật cần sửa vì chủ dự án + nhiều người dùng test bằng DevTools. Sửa: thêm `scrollbar-width:none` + `::-webkit-scrollbar{width:0;height:0}` CHỈ trong `body.dc-lock` (đúng lúc đang ở Đi Chùa), không ảnh hưởng scrollbar tuỳ biến của các trang sáng khác (đã verify `getComputedStyle` khác nhau giữa 2 ngữ cảnh) — cuộn/vuốt vẫn hoạt động bình thường, chỉ ẩn phần hiển thị thanh cuộn. 373 test.
- **v3.90 — Đi Chùa: sửa biển hiệu đè chữ trên mobile** (chủ dự án gửi screenshot DevTools iPhone 6/7/8 Plus 414px, phản hồi "xấu quá"): `.dc-sign` (biển hiệu "ĐẠI HÙNG BẢO ĐIỆN", neo giữa) và `.dc-caption` (hộp "Đang ở" + tên + mô tả, neo trái, max-width 300px) cùng `top:16px` — đủ chỗ ở desktop nhưng đè lên nhau khi khung hẹp dưới ~414px vì 300px đã chiếm gần hết chiều ngang màn hình. Sửa: ở `@media max-width:760px`, đẩy `.dc-caption` xuống `top:50px` + full-width (`left/right:10px`, bỏ max-width) để xếp DƯỚI biển hiệu thay vì cạnh nó; verify bằng đo rect chính xác qua dev server thật (sign bottom=231 vs caption top=236, không còn chạm), kiểm tra cả địa điểm có mô tả dài nhất (Chánh Điện, 129 ký tự) vẫn không đụng vùng nút điều khiển phía dưới. 373 test.
- **v3.89 — Audit + sửa UI mobile toàn site** (chủ dự án: "người dùng sẽ dùng mobile nhiều hơn"): quét lập trình (không phải đoán mắt) toàn bộ 16 route ở khổ ~375px — kiểm tra overflow ngang thật (loại trừ các khu vực cố tình overflow-x:auto như slider/bảng) và kiểm tra MỌI nút bấm có vùng chạm < 36px. Kết quả: không có lỗi vỡ layout ngang ở trang nào (sweep H08 cũ vẫn còn hiệu lực). Tìm + sửa 3 lỗi vùng chạm quá nhỏ: (1) nút đóng modal Đi Chùa `.dc-dialog-x` chỉ 15×26px → 44×44px; (2) nút đóng Modal chung (dùng ở Tarot/Kinh Dịch/...) không có vùng đệm → thêm w-11 h-11; (3) **78 nút yêu thích ☆/★ trên mọi lá Tarot chỉ 13×16px** (lỗi nặng nhất, lặp trên toàn bộ thư viện 78 lá) → 40×37px; (4) mũi tên lịch vạn niên trang chủ 27px ngang → 43px. 373 test.
- **v3.88 — Đi Chùa: sửa lỗi "vũng nước" mờ dưới chân lư hương + đổi ống xăm sang ảnh Dreamina AI (chủ dự án tự tạo, không tay người, nền studio sạch)**: phát hiện gốc lỗi — vùng phản chiếu xám dưới chân lư hương bị thuật toán xoá nền phân loại sai thành alpha=255 (vật thể đặc), không phải viền mờ như tưởng; sửa bằng dải lọc nghiêm ngặt riêng cho 30% đáy ảnh (yêu cầu chroma cao HOẶC rất tối HOẶC khoảng-cách-màu lớn mới giữ alpha, nếu không thì xoá hẳn) — xác minh bằng ảnh heatmap alpha (trắng=giữ, đen=xoá) trước/sau. Ống xăm: thay ảnh cũ (có vũng nước tương tự) bằng 2 tư thế mới từ ảnh do chủ dự án nhờ Dreamina AI tạo (không tay người, nền xám phẳng giống lư hương) — ảnh "yên" lúc chưa lắc + ảnh "xoè" lúc đang lắc, ShakeXam.jsx đổi ảnh theo `phase` (idle/shaking); chỉnh lại kích thước + vị trí khung `.xam-cyl`/`.xam-falling` cho khớp tỉ lệ ảnh mới (cao/hẹp hơn ảnh cũ). 373 test.
- **v3.87 — Đi Chùa: lư hương + ống xăm dùng ẢNH THẬT** (chủ dự án tự tạo bằng Gemini, theo prompt gợi ý — 2 ảnh đỉnh đồng + ống xăm tre chạm khắc rất chi tiết, nền xám phẳng): xử lý xoá nền hoàn toàn bằng Canvas API qua browser thật của dev server (preview_eval), KHÔNG thêm dependency. Thuật toán: nội suy màu nền theo 4 góc (bilinear) — nền ảnh studio có gradient nhẹ nên 1 màu nền cố định không đủ (phát hiện được khi so khớp nền xám tan vào object); ngưỡng khoảng cách màu mềm (alpha dốc tuyến tính giữa 2 mốc) cho biên đỡ răng cưa; tự crop theo bounding-box vùng alpha>ngưỡng; cắt thêm vùng góc dưới-phải để bỏ icon sparkle watermark của ảnh gốc. Verify xoá nền đúng bằng cách ghép thử lên nền xanh lá rực rồi tự xem qua Read tool (phát hiện 1 lần tưởng nhầm là chưa xoá — do nền xám gần giống checkerboard hiển thị, ghép lên xanh lá mới thấy rõ chính xác). Output lưu `public/dichua/dc-pot-cutout.png` (600×461) + `dc-tube-cutout.png` (600×533). Wiring: DiChua.jsx bỏ `.dc-pot/.dc-pot-h/.dc-pot-leg/.dc-incense/.dc-flame` (CSS vẽ tay) → 1 `<img>` + giữ `.dc-smoke` (khói bốc từ đỉnh nắp, ảnh thật đã có nắp đục lỗ nên bỏ luôn hiệu ứng lửa-trên-đầu-nhang không còn hợp ảnh); ShakeXam.jsx bỏ map 11 `.xam-stick` + `.xam-tube`/`.xam-mouth` → 1 `<img>` trong `.xam-cyl` (vẫn tilt/lắc nguyên cụm theo ảnh), giữ `.xam-falling` (que rời rơi ra) là CSS riêng vì đó là chi tiết phụ nhanh, không cần khớp pixel ảnh. Resize `.xam-cyl` 96×176→160×142 cho khớp tỉ lệ ảnh mới (ảnh chụp ngang hơn dự kiến vì que xoè rộng). Thử dùng `html-to-image` (dependency có sẵn) qua dev server để tự chụp ảnh DOM kiểm chứng — vướng CORS đọc font Google Fonts làm treo, bỏ hướng này, quay lại xác minh qua thuộc tính DOM (`complete`/`naturalWidth`) + chờ chủ dự án gửi ảnh chụp xác nhận lần nữa. 373 test pass (không đổi). Build main+single OK, NUL=0 (985861 byte); `tam-so-test.html` cập nhật. Bump sw v3.87 + package 3.87.0.
- **v3.86 — Đi Chùa: giảm độ tối hậu cảnh toàn trang** (chủ dự án gửi 3 ảnh chụp: sidebar/slider/right-panel vẫn đặc một màu đen, không thấy ảnh mờ đâu): nguyên nhân — `.dc-page-bg` (v3.85) dim quá tay, `brightness(.34)` + lớp phủ `::after` tối tới `rgba(11,18,32,.8)` cộng lại gần như xoá hết màu ảnh trước khi tới được các panel translucent (`.dc-sidebar`/`.dc-card` chỉ 3-4% trắng nên không tự sáng lên được). Tăng `brightness(.34→.7)`, giảm lớp phủ tối `(.8→.5 max, .25→.05 min)` — ảnh giữ đủ sáng để thấy màu/hoạ tiết qua các panel, vẫn đủ tối để chữ đọc được. KHÔNG tự xem được bằng mắt (screenshot môi trường này vẫn treo dù thử lại) — cần chủ dự án tự xem & xác nhận lần nữa. 373 test pass (không đổi). Build main+single OK, NUL=0 (986097 byte); `tam-so-test.html` cập nhật. Bump sw v3.86 + package 3.86.0.
- **v3.85 — Đi Chùa: hậu cảnh mờ cho TOÀN TRANG** (chủ dự án sửa ý v3.84: "ý tôi background toàn trang á" — không phải chỉ riêng modal Xin Xăm): thêm `.dc-page-bg` — ảnh thật của khu đang xem (đổi theo `loc.id`, blur(30px)+brightness(.34)+overlay radial-gradient tối) đặt làm hậu cảnh cho TOÀN BỘ `.dc-root` (z-index:-1, sau header/sidebar/stage/right-panel/slider) — trước đó page chỉ có nền xanh đen phẳng `#0b1220` ngoài vùng viewport trung tâm. Sidebar/card phải (đều có nền translucent rgba trắng nhạt) giờ thấy ảnh mờ phía sau, đồng bộ với viewport trung tâm đang hiện ảnh sắc nét — cảm giác cả màn hình "ngập" trong không khí của khu đang xem. Verify qua dev server thật: kiểm thuộc tính `style` trực tiếp (không dùng `getComputedStyle` vì dính lại lỗi mô phỏng transition của môi trường test) xác nhận đổi đúng theo khu (chanh-dien.jpg → thap-chuong.jpg khi đổi sidebar); z-index đúng (header/logo vẫn ở trên, kiểm bằng `elementFromPoint`); không lỗi console. **Về lư hương/ống xăm**: chủ dự án phản hồi 2 lần (v3.83 CSS nâng cấp vẫn "xấu quá") — đã nói rõ CSS vẽ tay có giới hạn, đề xuất hướng ảnh thật (như 11 ảnh cảnh chùa) cho 2 phần tử này, chờ chủ dự án quyết định có muốn tự tạo ảnh không. 373 test pass (không đổi). Build main+single OK, NUL=0 (986097 byte); `tam-so-test.html` cập nhật. Bump sw v3.85 + package 3.85.0.
- **v3.84 — Đi Chùa: ảnh thật làm hậu cảnh mờ cho modal Xin Xăm** (theo phản hồi screenshot "vẫn vậy mà, với background chỉnh như hình trước đó"): nâng cấp CSS v3.83 chưa đủ — chuyển hướng dùng ẢNH THẬT đã có sẵn (11 ảnh Gemini ở public/dichua/) làm hậu cảnh mờ cho khung thoại, thay nền xanh đen phẳng `#121a2b`. `DcModal` thêm prop `bgImage` tuỳ chọn → render `.dc-dialog-bg` (ảnh blur(20px)+brightness(.45) phủ lớp gradient tối để chữ vẫn đọc được) đặt SAU nội dung; nội dung tách sang `.dc-dialog-content` (giữ scroll/padding cũ, `.dc-dialog` chỉ còn vai trò khung cắt blur tràn viền). Modal Xin Xăm dùng ảnh đúng khu đang xem (`loc.id`) — đứng ở Chánh Điện thì hậu cảnh là ảnh Chánh Điện, đổi khu thì hậu cảnh đổi theo. Verify qua dev server thật: ảnh tải thành công (1376×768, kiểm bằng Image() preload), `getComputedStyle` xác nhận `backgroundImage` đúng URL, không lỗi console. 373 test pass (không đổi). Build main+single OK, NUL=0 (985602 byte); `tam-so-test.html` cập nhật. Bump sw v3.84 + package 3.84.0.
- **v3.83 — Đi Chùa: nâng cấp chất liệu lư hương + ống xăm (CSS, không thêm asset/dependency)**: theo phản hồi "ống xăm và bát hương xấu quá" — không có công cụ tạo ảnh nên KHÔNG thể tạo ảnh thật như 11 ảnh cảnh chùa (Gemini, do chủ dự án tự làm trước đó); 3D thật (Three.js) đã chủ động không dùng từ v3.76 (lệch kiến trúc web tĩnh). Chọn hướng nâng cấp CSS vẽ tay: thêm lớp **radial-gradient lệch tâm** mô phỏng ánh sáng bắt trên mặt trụ tròn (mẹo CSS kinh điển để giả độ cong — trước đó chỉ có linear-gradient phẳng) cho cả `.dc-pot` (lư hương), `.xam-tube` (ống xăm) và `.xam-stick`/`.xam-falling` (que xăm); thêm "đốt tre" (vòng tối ngang ống, mô phỏng mắt tre thật) cho `.xam-tube`; vân gỗ tự nhiên hơn (2 lớp repeating-gradient chu kỳ khác nhau thay vì 1 lớp đều tăm tắp) + `mix-blend-mode: overlay`; quai + chân lư hương thêm box-shadow tạo độ nổi khối. Verify: không có lỗi console, `getComputedStyle` xác nhận gradient mới áp đúng cho cả 3 phần tử (.dc-pot/.xam-tube/.xam-stick) qua dev server thật — KHÔNG tự xem được bằng mắt trong môi trường này (screenshot vẫn treo), đã nói rõ với chủ dự án cần tự mở localhost xem. 373 test pass (không đổi — thuần màu/gradient). Build main+single OK, NUL=0 (985005 byte); `tam-so-test.html` cập nhật. Bump sw v3.83 + package 3.83.0.
- **v3.82 — Đi Chùa: Lịch Sử Của Bạn** (nâng cấp theo yêu cầu chủ dự án, ưu tiên DC09 "lịch sử lời nguyện/xăm đã lưu"): Lời Nguyện đã LƯU sẵn vào localStorage từ v3.76 nhưng chỉ hiện SỐ LƯỢNG, không xem lại được; Xin Xăm thì không lưu gì cả (mỗi lần rút là mất). Thêm: (1) `addXamLichSu/loadXamLichSu/clearXamLichSu` trong `dichua.js` (mirror đúng pattern `addLoiNguyen` đã có — lưu `{t, so}` tối đa 50 lượt) + `clearLoiNguyen`; (2) hook `ShakeXam.jsx` lưu lịch sử ngay khi que rơi; (3) nút mới "📜 Lịch Sử Của Bạn" ở sidebar footer DiChua.jsx mở modal hiện 2 danh sách (Lời Nguyện đã viết + Thẻ Xăm đã rút, có giờ-ngày, mới nhất trước) kèm nút "Xoá hết" riêng từng mục; tra lại nội dung thẻ xăm qua `xamBySo` có sẵn. Giữ đúng giọng minh bạch của site: "Lưu riêng trên máy bạn, không gửi đi đâu. Xoá bất cứ lúc nào." CSS mới `.dc-history-*` (khối Đi Chùa, theme tối riêng). +6 test (367→373: data layer + wiring DiChua.jsx/ShakeXam.jsx). Verify qua dev server thật (không chỉ đọc code): viết lời nguyện → mở Lịch Sử thấy đúng nội dung+giờ; rút xăm qua nút phụ → Lịch Sử thấy "Thẻ số 2 · Trung Bình — câu thơ"; bấm Xoá hết cả 2 mục → về đúng trạng thái rỗng + thẻ panel chính reset số đếm. Build main+single OK, NUL=0 (983906 byte); `tam-so-test.html` cập nhật. Bump sw v3.82 + package 3.82.0.
- **v3.81 — Dark mode toggle** (LOW backlog, theo yêu cầu chủ dự án "làm phần không tốn phí trước"): đổi 7 token màu chung (`ink/ink2/gold/clay/plum/cream/muted`) trong `tailwind.config.js` sang tham chiếu biến CSS `rgb(var(--c-x) / <alpha-value>)` + `darkMode:'class'`; định nghĩa giá trị sáng (`:root`)/tối (`.dark`) trong `index.css`, thay ~25 chỗ hex cứng trong `.panel/.btn-primary/.badge/.field-input/.tcard`... sang dùng biến — class cũ ở MỌI trang tự đổi màu, không cần sửa JSX từng trang. Thêm `ThemeToggle.jsx` (nút ☀/☾, lưu `localStorage.tamso-theme`) gắn vào header; script chống nhấp nháy (FOUC) trong `index.html` áp đúng theme TRƯỚC khi vẽ trang; `meta theme-color` tách sáng/tối. Bổ sung biến thể `dark:` cho ~30 chỗ dùng màu cảm xúc (`text-emerald-800/text-rose-700/text-amber-800` dùng làm chữ trần không có nền riêng — tối lại không đọc được) ở 12 file, + 4 chỗ `text-black/border-black` (mờ/disabled) → `dark:text-white/...`. KHÔNG đổi 2 nhóm: badge Miếu-Vượng-Đắc-Bình-Hãm + sáng/tối ở LaSoTuVi (màu phân biệt có chủ đích, tự mang nền riêng) và trang **Đi Chùa** (`.dc-*`, theme tối RIÊNG cố định, độc lập hệ toggle này). **Gotcha merge**: phiên này pull về 7 commit mới (v3.76–3.80, tính năng Đi Chùa) NGAY GIỮA lúc đang sửa dark mode dở; `git checkout stash@{0} -- <file>` GHI ĐÈ THÔ (không merge) nên làm mất link "Đi chùa" mới ở Layout.jsx + favicon mới ở index.html — phát hiện nhờ test fail ngay, sửa bằng `git checkout HEAD -- file` rồi áp lại đúng diff của mình qua safe-edit. Bài học: dùng `git stash pop` (3-way) hoặc so diff kỹ trước khi `checkout stash@{0} -- file` trên file cả hai bên cùng sửa. **Gotcha xác minh**: lúc đầu tưởng body không đổi màu khi toggle — hoá ra do `transition: background-color` không tua được trong tab headless của môi trường preview (đọc `getComputedStyle` kẹt ở giữa transition); tắt tạm transition thì đổi đúng ngay — KHÔNG phải bug thật, đã verify lại qua nhiều trang (Tarot 78 lá, La Số Tử Vi, header, nút yêu thích) đều đúng giá trị dark. 367 test pass. Build /c/tamso-build-v381 main+single OK, NUL=0 (980817 byte); `tam-so-test.html` cập nhật. Bump sw v3.81 + package 3.81.0.
- **v3.80 — Đi Chùa: thắp hương theo khu + hiệu ứng đốt + lắc xăm 7–30s + sửa que tre**: (1) **Chỉ thắp hương nơi có ban thờ**: thêm cờ `huong:true` cho 3 khu (Chánh Điện, Nhà Tổ, Tượng Cầu Nguyện) trong dichua.js. Thẻ "Thắp Hương Online": khu có hương án → hiện đỉnh đồng + nút "Thắp Hương tại <khu>"; khu KHÔNG có (Thư Viện, Vườn, Cổng…) → ẩn nút, hiện lời nhắc + chip dẫn tới 3 nơi thắp được. (2) **Thêm thao tác & hiệu ứng**: bấm thắp → ngọn lửa bùng (`.dc-flame` keyframe dcFlame ~1.3s) ở đầu nhang → đầu nhang cháy đỏ phát sáng; đồng thời **khói bốc trong CẢNH** (viewport) tại khu đã thắp (`.dc-scene-smoke` 4 luồng bay lên), lưu theo session (litLocs). Nút khoá khi đang đốt (`disabled={igniting}`), note đổi theo khu. (3) **Lắc xin xăm 7–30s**: ShakeXam.jsx viết lại — chọn mốc NGẪU NHIÊN `7000+rand*23000` ms; vòng lặp requestAnimationFrame chỉ cộng thời gian khi ĐANG lắc (có hoạt động lắc trong 420ms gần nhất); thanh tiến độ = đã-lắc/mốc, hiện số giây "(đã lắc Xs)"; đủ mốc mới rơi que. Lắc = kéo pointer hoặc DeviceMotion (mỗi cử động đăng ký 1 nhịp lắc). Nút phụ a11y vẫn cho rơi ngay. (4) **Que tre bớt giống que diêm**: bỏ "đầu diêm" tròn đỏ → đầu SƠN SON phẳng cùng bề ngang + chỉ vàng phân cách, thân tre dẹt vân, các que XOÈ nhẹ (rotate theo --n). Verify Playwright thật: Chánh Điện có nút thắp + khói cảnh; Thư Viện hiện 3 chip không nút; lắc 3.5s CHƯA rơi (progress 17%, "đã lắc 3s" → mốc ~20s, đúng tối thiểu 7s); fallback cho kết quả; 0 console error. 367 test pass. Bump sw v3.80 + package 3.80.0.
- **v3.79 — Đi Chùa: nâng cấp ống xăm + lư hương cho nhìn thật hơn + LOGO MỚI**: (1) **Ống xăm**: thay khối CSS phẳng bằng ống GỖ có vân (gradient trụ + vân dọc + đai đồng), miệng ống vành đồng (radial), que TRE đầu sơn ĐỎ (gradient bamboo + ::before đỏ), vẫn giữ lắc/nghiêng/rơi. (2) **Lư hương** (thẻ Thắp Hương): thay bằng ĐỈNH ĐỒNG — nồi brass gradient + miệng + 2 quai (ring) + 2 chân + bóng đổ; 3 cây nhang nghiêng quạt, đầu nhang cháy đỏ phát sáng khi thắp; 3 luồng khói lệch nhau (animation). Đổi markup ShakeXam.jsx (que cao hơn, tube/mouth thay cup) + DiChua.jsx (dc-pot/dc-incense/dc-smoke thay bowl/stick), bỏ khói cũ qua ::after. (3) **LOGO MỚI**: chủ dự án đưa `logo.png` (huy hiệu tròn rồng + bút lông + chữ "Tam Sở" trên đĩa đá). Dùng headless Chromium/canvas (không thêm dependency) nhận diện đĩa qua pixel bão hoà/tối (loại nền trắng + tấm nền mờ + bóng), cắt vuông quanh đĩa + mask TRÒN nền trong suốt → `public/logo.png` (256px, ~70KB). Viết lại `Logo.jsx` render <img> logo.png (giữ API size/className) → dùng được trên nav nền sáng lẫn header Đi Chùa nền tối; favicon index.html → ./logo.png. Verify Playwright thật: logo hiện ở nav (/logo.png) + header tối, ống xăm gỗ + que đỏ, đỉnh đồng + khói, 0 console error. 367 test pass. Bump sw v3.79 + package 3.79.0.
- **v3.78 — "Đi Chùa": gắn ẢNH THẬT (do Gemini tạo) + đổi tên chùa → "Chùa Tam Sở"**: chủ dự án đã tạo 11 ảnh photoreal bằng Gemini (theo bộ prompt đã đưa) và thả vào `public/dichua/<id>.jpg`. SceneView (làm sẵn ở phiên trước) tự hiển thị ảnh thật, biển hiệu overlay đè lên (Chánh Điện "ĐẠI HÙNG BẢO ĐIỆN", Cổng Tam Quan "CHÙA TAM SỞ", Tượng "TƯỢNG QUÁN ÂM"…). Đổi tên chùa "Chùa An Lạc" → **"Chùa Tam Sở"** ở mọi nơi (header brand, SeoTag, alt, modal Công Đức, aria-label TempleScene, bienHieu cổng, README). **TỐI ƯU ẢNH**: 11 ảnh từ Gemini rất nặng (2.5–4.9MB/tấm, tổng 36.3MB) → dùng headless Chromium (Playwright, KHÔNG thêm dependency) re-encode bằng canvas: downscale ≤1600px (ra 1376×768 ~16:9), JPEG q82 → **36.3MB → 2.87MB** (mỗi tấm ~210–303KB). Verify Playwright thật: trang giờ hiển thị ẢNH photoreal (stage img=1, svg fallback=0), brand "Chùa Tam Sở", biển hiệu đúng, 0 console error — đẹp sát mockup. 367 test pass (data đổi chữ không ảnh hưởng assertion). Bump sw v3.78 + package 3.78.0. (SceneView vẫn giữ fallback SVG nếu sau này thiếu ảnh.)
- **v3.77 — "Đi Chùa" làm lại theo yêu cầu: full-page + tự vẽ cảnh + lắc xin xăm** (theo ảnh mockup "Chùa An Lạc"). (1) **FULL PAGE**: chuyển route `/di-chua` ra NGOÀI `<Layout>` trong main.jsx → trang chiếm trọn màn hình (`.dc-root` position:fixed inset-0), có **header riêng** đúng ảnh (logo "Chùa An Lạc / Không gian tâm linh online" + nav Trang Chủ·Chùa Online·Kinh Sách·Phật Pháp·Sự Kiện·Cộng Đồng + 🔍🔔 + avatar "Phật Tử/Hồ Sơ"), sidebar 11 khu, viewport, right panel, slider — không còn nav/footer của site chính. Khoá scroll body khi mở. (2) **TỰ TẠO HÌNH ẢNH**: `TempleScene.jsx` — vẽ cảnh chùa hoàn toàn bằng **SVG** (KHÔNG ảnh chụp/bản quyền): trời theo tông (dawn/day/dusk/gold) + mặt trời/trăng + mây + 2 lớp núi + cây + sân phối cảnh + công trình thay theo `scene` (mái cong nhiều tầng cho Chánh Điện/hội trường, **cổng tam quan**, **tháp chuông** nhiều tầng, **tượng Quán Âm**, **hồ sen** cho vườn Lâm Tỳ Ni, **vườn đá** Góc Thiền) + lư hương khói + đèn đá + biển hiệu. Mỗi instance có id gradient riêng. Thumbnail slider cũng render mini-scene. Đã nói rõ với chủ dự án: không xuất được ảnh photoreal như mockup → dùng minh hoạ SVG (đúng bố cục, sạch bản quyền). (3) **XIN XĂM PHẢI LẮC**: `ShakeXam.jsx` — ống xăm + que; **lắc mới rơi**: kéo ống qua lại (pointer, desktop+cảm ứng) đếm số lần ĐỔI CHIỀU, hoặc lắc điện thoại thật qua **DeviceMotion** (có nút xin quyền cho iOS). Đủ 100% → 1 que rơi (animation) → hiện thẻ (16 thẻ, diễn giải tham khảo) + "Xin thẻ khác". Có nút phụ a11y cho người không lắc được. Tôn trọng reduced-motion. 3 thẻ phải vẫn theo ảnh: Lời Cầu Nguyện (lưu local) · Thắp Hương Online (lư hương + khói CSS) · Công Đức (modal minh bạch: CHƯA có thanh toán thật) + thêm thẻ Xin Xăm. Các mục nav phụ/🔍/🔔 mở modal "đang phát triển" (không giả vờ chạy). **367 test pass** (+3, sửa 3 test cũ: 11 khu, route full-page ngoài Layout, có TempleScene/ShakeXam). Verify bằng **Playwright thật** (10 ảnh: full page + 4 scene khác nhau + lắc ra xăm "Thẻ số 4·Hạ" + help + mobile, 0 console error). **Bug đã sửa khi verify**: drop-timer của xăm đặt trong useEffect keyed theo `phase` → cleanup tự huỷ timer khi phase đổi 'shaking'→'dropping', kẹt ở "đang rơi" mãi → chuyển sang quản lý bằng `useRef` (progRef/dropTimer/dropped) ngoài effect. Thay TRỌN khối CSS `.dichua-*` (v3.76) bằng khối `.dc-*` + `.xam-*` full-page (dùng literal màu/font, không `theme()` ngoài @layer). Build dev sạch (~820KB do +scene SVG). Bump sw v3.77 + package 3.77.0.
- **v3.76 — Tính năng mới "Đi Chùa" (Đại Tự Tâm Linh, MVP)** theo `di-chua.md` (đề bài "Thế giới tâm linh 3D") + ảnh mockup tham khảo. Quyết định phạm vi: làm MVP đi thẳng vào layout giống ảnh, **KHÔNG** kéo theo Three.js/TypeScript/Next.js như đề bài đề xuất — lệch hẳn kiến trúc hiện tại (web tĩnh JS, client-side, không backend) và rủi ro cao cho một thử nghiệm chưa kiểm chứng nhu cầu thật; viewport dùng minh hoạ emoji+gradient (không ảnh thật) để né vấn đề bản quyền địa danh & không cần asset 3D. Route `/di-chua` (`DiChua.jsx`) + data thuần `dichua.js`: 10 địa điểm (3 MVP — Cổng Tam Quan/Sân Chùa/Chánh Điện — + 7 "sắp ra mắt"), 16 thẻ xăm **tự biên soạn** + `rutXam`/diễn giải tham khảo (đổi "AI giải xăm" trong đề bài → diễn giải tất định vì không có backend/API thật, tránh nhận vơ là AI). 3 thẻ hành động: Khấn Nguyện (lưu localStorage), Thắp Hương (đếm tượng trưng + khói CSS), Xin Xăm. "Công Đức" trong ảnh đổi thành modal minh bạch nói rõ CHƯA có thanh toán thật (không dựng nút donate giả). UI tông tối riêng (scoped `.dichua-*`, không đụng theme sáng toàn site) mô phỏng đúng bố cục ảnh: sidebar địa điểm · viewport + nút hướng/zoom · slider ảnh dưới · panel hành động bên phải. +16 test (348→364). Verify bằng **Playwright thật** (không chỉ đọc code) — phát hiện & sửa 2 lỗi: (1) thiếu logic ẩn 3 thẻ hành động ở địa điểm "sắp ra mắt" (JSX sai: 3 `<div>` không bọc fragment khiến `{loc.mvp && ...}` chỉ đóng ở div đầu — sửa lại bằng `<>...</>`); (2) `.dichua-hint` (chữ chú dẫn góc trái) đè lên vùng nút điều hướng ở viewport hẹp, chặn click — thêm `pointer-events:none`. Môi trường: `framer-motion` có khai trong package.json nhưng thiếu trong `node_modules` (Windows) → cài lại mới build được; cài Playwright + Chromium tạm trong scratchpad (ngoài repo) để chụp 10 ảnh chứng minh UI thật chạy đúng, 0 console error. Build dev-mode sạch; bump sw v3.76 + package 3.76.0.
- **v3.75 — Hoàn tất V03 (tinh chỉnh UI, theo yêu cầu "làm cho xong luôn")**: từ báo cáo audit, lọc còn 2 điểm THẬT (bỏ 1 false-positive): (1) **Hex cứng → token**: nút filter active ở Tarot.jsx (lọc Ẩn Chính/chất) và Collection.jsx (lọc loại) dùng `bg-[#b45309]` literal → đổi `bg-gold` (cùng giá trị, chỉ là dùng đúng token — verify computed bg vẫn `rgb(180,83,9)`). (2) **Lệch cỡ chữ H1 trang con**: rà 6 trang "detail" (Tarot CardPage, Numerology SoChuDaoPage, SinhNam, HopTuoi, ConGiap, Zodiac CungPage) thấy 4/6 đã tự nhất quán quanh `clamp(1.6-1.8rem,4-4.5vw,2.4-2.6rem)`, chỉ ConGiap+Zodiac lệch hẳn ra `clamp(2rem,5vw,3rem)` (to hơn ~20% trên desktop) dù cùng có symbol/số to 3.6rem làm điểm nhấn như Numerology — đổi cả 2 về `clamp(1.8rem,4.5vw,2.6rem)` (khớp HopTuoi, mức cao nhất của cụm 4 trang). Verify qua dev server thật (`preview_eval` lấy `getComputedStyle` font-size): ConGiap 42.9px, Tarot CardPage 41.25px — chênh ~4% (trước đó lệch ~20%). **BỎ QUA** badge sáng/tối/Miếu-Vượng ở LaSoTuVi.jsx (DO_CLS legend 5 màu + badge 3 màu): xác minh lại đây là hệ màu phân biệt CÓ CHỦ ĐÍCH (mỗi mức một màu để dễ phân biệt), không phải tàn dư theme cũ — sửa sẽ làm MẤT khả năng phân biệt, không sửa. 348 test pass. Build /c/tamso-build-v375 main+single OK, NUL=0 (920064 byte); `tam-so-test.html` cập nhật. Bump sw v3.75 + package 3.75.0.
- **v3.73 (tiếp) — Tinh chỉnh UI: vá "lớp phủ trắng mờ" vô hình**: theo yêu cầu chủ dự án "tinh chỉnh UI". Phát hiện bằng suy luận màu (không cần ảnh chụp màn hình — toán cộng màu): nhiều nơi dùng `bg-white/[.03-.05]` làm nền "ô nổi nhẹ" — pattern này hợp lý trên nền **tan/giấy-cổ cũ** (`ink` = `#efe2c4`, phủ trắng mờ làm sáng lên), nhưng từ v3.72 `ink` đã đổi thành **trắng tinh** (`#ffffff`) → trắng phủ lên trắng = vô hình, các ô mất hẳn nền phân biệt, chỉ còn viền vàng mảnh. Đổi toàn bộ `bg-white/[.0x]` → `bg-gold/5` (khớp pattern đã đúng sẵn ở LaSoTuVi.jsx:209) — ảnh hưởng: **lưới 78 lá Tarot** (Tarot.jsx), 4 ô kết quả widget "Hôm nay" (Today.jsx — trang chủ), accordion FAQ (Sources.jsx), accordion bộ số + lưới Lo Shu (Numerology.jsx), lưới hào Mai Hoa (IChing.jsx), ô lá số Tử Vi (LaSoTuVi.jsx) — 9 chỗ / 6 file. Verify bằng `preview_inspect` (getComputedStyle qua dev server thật, screenshot bị treo trong môi trường này): nền đổi từ trắng-trên-trắng → `rgba(180,83,9,.05)` khớp viền `rgba(180,83,9,.2)`. Gặp thêm 2 vấn đề môi trường khi dựng dev/preview: (1) `node_modules` trên D: thiếu `framer-motion` đã khai trong package.json nhưng KHÔNG có trong package-lock.json (lockfile thiếu đồng bộ từ v3.72) → `npm install` lại để khoá đúng; (2) dùng `.claude/launch.json` + Claude Preview MCP để chạy dev server thật và kiểm computed style (an toàn hơn build tĩnh vì thấy được kết quả runtime). 348 test pass (không đổi — đổi màu thuần). Sửa qua `safe-edit.mjs`, toàn vẹn OK. Build /c/tamso-build-v373 (vị trí ngoài thư mục home để né file package.json rỗng lạ) main+single OK, NUL=0 (920221 byte); `tam-so-test.html` cập nhật.
- **v3.73** — **Vá màu giấy-cổ sót lại sau ĐẠI TU v3.72**: đợt đổi token v3.72 chỉ đổi class Tailwind nên 3 nơi dùng **inline style** (không qua token) vẫn giữ gradient kem/tan cũ, lệch tông trắng/slate mới — `Modal.jsx` (nền hộp thoại + lớp phủ tối), `CardImage.jsx` (nền fallback khi lá Tarot chưa có ảnh), `Skeleton.jsx` (shimmer khi đang tải, dùng ở RẤT nhiều nơi). Đổi cả 3 sang gradient trắng/slate (#fff, #f8fafc/#eef2f7, #eef2f7/#e2e8f0) + lớp phủ modal từ nâu đậm (#0d0803) sang slate-900/60 trung tính. Thuần màu/inline-style, không đổi logic. **348 test pass** (không đổi — không có assertion theo màu). Sửa qua safe-edit.mjs, cả 3 file Toàn vẹn OK. Build verify: gặp & né file package.json rỗng (0 byte) lạ ở thư mục home user (Windows) làm esbuild/vite crash khi build ở bất kỳ thư mục con nào của home dir (walk ngược cây cha); build thành công khi dựng ở thư mục mới ngoài home dir (gốc ổ C:). Single-file NUL=0 (920447 byte); tam-so-test.html cập nhật. Bump sw v3.73 + package 3.73.0.
- **v3.72** — **ĐẠI TU UI: clean-SaaS sáng + Framer Motion (toàn site)** theo yêu cầu chủ dự án. (1) **Design tokens** (tailwind.config + index.css): đổi tông từ giấy-cổ/vàng-nâu → **nền trắng/slate, nhấn hổ phách (amber)**, bóng mềm trung tính, bo góc hiện đại, heading chuyển sang **sans** (Be Vietnam Pro) chữ đậm/tracking âm. Đổi GIÁ TRỊ token giữ TÊN cũ (cream→slate-900 chữ chính · ink/ink2→bề mặt trắng · gold→amber) ⇒ mọi trang dùng chung `.panel`/`.btn`/`text-cream`… tự đổi theo. Viết lại `.panel` (trắng+viền slate+shadow mềm), `.btn-primary` (amber đặc), `.btn-ghost`, `.badge`, `.field-input` (focus ring), `.disclaimer`, `.note`, bảng, navlink, scrollbar. (2) **Framer Motion**: thêm dependency; **chuyển trang mượt toàn site** (`motion.main` fade+slide theo route trong Layout, tôn trọng reduced-motion); header đổi trắng mờ + viền slate; nút nhấn lún (`:active` scale). (3) Chuẩn hoá: bỏ "viên thuốc gradient vàng" cũ ở Collection/Tarot/Lá số → nhấn amber đặc đồng bộ. Giữ Reveal/stagger (hiệu ứng cuộn) sẵn có. **348 test pass** (thuần style/markup). Sửa qua node fs/safe-edit (mọi file Toàn vẹn OK). Build /tmp/b main+single OK (NUL=0, bundle ~920KB do +framer-motion); tam-so-test.html cập nhật; bump sw v3.72 + package 3.72.0. **Đây là đợt NỀN TẢNG — chờ chủ dự án duyệt hướng rồi tinh chỉnh chi tiết từng trang.**
- **v3.71** — **Lịch vạn niên trên Trang chủ** (theo yêu cầu): component `LichVanNien.jsx` — khối hai cột **Dương lịch | Âm lịch** (số ngày lớn), can chi **Ngày · Tháng · Năm**, **Giờ hoàng đạo**, và nút **‹ ›** để xem ngày khác (+ "Về hôm nay"). Thêm hàm thuần `monthCanChi(lunarYear, lunarMonth)` (luật Ngũ Hổ Độn) vào tuvi.js — đã verify khớp ảnh mẫu: 22/06/2026 = Ngày Đinh Mão · Tháng Giáp Ngọ · Năm Bính Ngọ. Tông giấy-cổ/vàng-nâu của site (không bê màu xanh từ ảnh). Gắn ngay sau hero ở Home. **348 test pass** (+8: monthCanChi khớp ảnh + chi tháng + wiring). Sửa qua `safe-edit.mjs`/node fs (đều Toàn vẹn OK). Build /tmp/b main+single OK (NUL=0); tam-so-test.html cập nhật; bump sw v3.71 + package 3.71.0.
- **v3.70** — **Nav gọn lại bằng dropdown** (theo yêu cầu): desktop chỉ hiện 4 mục chính **Tarot · Thần số học · Tử vi · Kinh Dịch** + nút **"Thêm ▾"** thả xuống chứa 6 mục còn lại (Hồ sơ, Lá số Tử Vi, So đôi lá số, Cung hoàng đạo, Tương hợp, Nguồn); Trang chủ truy cập qua logo. Component `MoreDropdown` (click-ngoài/Escape/đổi route để đóng; nút "Thêm" sáng khi đang ở 1 trong các mục con; chevron xoay). Mobile (☰) GIỮ NGUYÊN liệt kê đủ 11 mục — không giấu gì. Tách container desktop (inline) vs mobile (panel). PRIMARY/MORE suy ra từ mảng `links` (không trùng lặp). **340 test pass** (thuần markup). Sửa qua node fs (ghép khối Navbar mới, verify cân ngoặc/độ dài). Build /tmp/b main+single OK (NUL=0); tam-so-test.html cập nhật; bump sw v3.70 + package 3.70.0.
- **v3.69** — **Làm đẹp thanh điều hướng (nav)** theo yêu cầu chủ dự án (giữ đủ 11 mục, làm phẳng & đẹp lại, KHÔNG gom nhóm): bỏ kiểu "viên thuốc gradient vàng-nâu" của mục đang chọn → thay bằng **gạch chân vàng tối giản** kiểu biên tập (hover trượt vào mượt, mục active = chữ vàng đậm + gạch chân); canh khoảng cách thoáng hơn (desktop gap-x 18px, căn giữa theo trục); nút Bộ sưu tập đổi active sang chữ vàng + nền vàng nhạt (đồng bộ, bớt nặng); menu mobile xổ dọc: mục active dùng nền vàng nhạt bo góc thay gạch chân. Thêm bộ class `.navlink` vào index.css (tôn trọng prefers-reduced-motion + focus-visible). **340 test pass** (không đổi — thuần CSS/markup). Sửa qua `safe-edit.mjs` (Layout.jsx + index.css đều Toàn vẹn OK). Build /tmp/b main+single OK (NUL=0); tam-so-test.html cập nhật; bump sw v3.69 + package 3.69.0.
- **v3.68** — **Dệt "quẻ hôm nay" (`dayWeave.js` → `weaveDay`)**: trên thẻ "Hôm nay" (Trang chủ — component lưu lượng cao nhất), khi rút lá Tarot + gieo quẻ Dịch, thêm khối **"Dệt lại — lá & quẻ nói gì cùng nhau"** nối hai hệ thành một lời gợi mở duy nhất, **bắc cầu Đông–Tây bằng ngũ hành**: nguyên tố phương Tây của lá (Lửa/Nước/Khí/Đất → Hỏa/Thủy/Mộc/Thổ) đối chiếu hành của quái trên quẻ Dịch → tương sinh/tương khắc/tỉ hòa. Lá Ẩn Chính (không có chất) → đọc như "hai lăng kính". Giọng tham khảo, nhấn mạnh "không phải lời phán về số phận". **340 test pass** (+8: ×500 lượt rút rà over-claim/khung/đúng nhánh + check ngũ hành Lửa×Kim khắc, Thủy×Mộc sinh + wiring). Sửa file qua `safe-edit.mjs` (helper mới) — Today.jsx & run.mjs đều "Toàn vẹn OK", KHÔNG còn cắt cụt. Build /tmp/b main+single OK (NUL=0); tam-so-test.html cập nhật; bump sw v3.68 + package 3.68.0.
- **v3.67** — **Diễn giải tổng hợp bộ số Thần số học (`numerologySynth.js` → `weaveNumbers`)**: dệt Số Chủ Đạo · Vận Mệnh · Linh Hồn · Nhân Cách · Trưởng Thành + Lo Shu khuyết thành một đoạn chiêm nghiệm — con đường↔hành trang (nhận diện cộng hưởng khi Chủ Đạo=Vận Mệnh), mong muốn bên trong (Linh Hồn)↔vẻ ngoài (Nhân Cách), điểm hội tụ nửa sau đời (Trưởng Thành), và các số Lo Shu còn khuyết như nét đáng rèn (KHÔNG coi là khiếm khuyết cố định). Render ở công cụ "Bộ số đầy đủ" (Numerology.jsx). Giọng tham khảo, KHÔNG over-claim. **332 test pass** (+7). ⚠ Edit TOOL ổ D: lại cắt cụt Numerology.jsx khi chèn → vá bằng node fs (ghép lại từ HEAD). Build /tmp/b main+single OK (NUL=0); tam-so-test.html cập nhật; bump sw v3.67 + package 3.67.0.
- **v3.66** — **Diễn giải tổng hợp cho Kinh Dịch (`ichingSynth.js` → `weaveCast`)**: dệt một đoạn chiêm nghiệm nối **quẻ chính → hào động (ngôi hào) → quẻ biến**, theo khung đầu→giữa→cuối (tình thế hiện tại · trọng tâm chuyển động · chiều đi tới). Xử lý 3 nhánh: không hào động (tình thế ổn định, KHÔNG bịa quẻ biến) · một hào động (nêu đúng ngôi hào) · nhiều hào động (lấy hào thấp/cao nhất). Giữ giọng tham khảo, KHÔNG phán chắc. Render trên IChing.jsx cả phần **gieo xu** lẫn công cụ **Mai Hoa**. **325 test pass** (+8: ×400 lượt gieo rà over-claim/khung/đủ dày + nêu quẻ chính & quẻ biến + 3 nhánh hào động + wiring). ⚠ Gotcha tái xác nhận: Edit TOOL trên ổ D: cắt cụt CẢ tests/run.mjs LẪN IChing.jsx khi append → vá lại bằng node fs (cắt phần đuôi hỏng + ghi lại). Build /tmp/b main+single OK (NUL=0); tam-so-test.html cập nhật; bump sw v3.66 + package 3.66.0.
- **v3.65** — **Diễn giải tổng hợp đa hệ + tinh chỉnh chuyển động**: (1) **Hồ sơ** thêm đoạn "Sợi chỉ chung" (`weaveProfile` trong report.js) dệt Số Chủ Đạo↔Vận Mệnh (cùng/khác xu hướng nội–ngoại), năm cá nhân + đỉnh vận, và hai lăng kính Đông–Tây — render dạng trích dẫn viền vàng đầu báo cáo. (2) **So đôi lá số** thêm `soLaSoSynth.js` (`weavePair`) dệt Mệnh (nhận diện vô chính diệu), cung Phu Thê, quan hệ địa chi — GIỮ ĐÚNG ràng buộc: nói rõ KHÔNG kết luận hợp/không hợp. (3) Tinh chỉnh stagger nhanh hơn (32ms/.5s) + media query ≤520px nhẹ & nhanh hơn (24ms/.42s, page-enter .36s). Sửa lỗi escape `\d`→`d` trong regex strip tiêu đề năm. **317 test pass** (+6, gồm rà over-claim + bắt buộc khung "không kết luận hợp"). Build v3.65.
- **v3.64** — **U06 lan toả hiệu ứng**: stagger lưới khi cuộn cho ConGiap (12 con giáp), HopTuoi (12 tuổi), 3 thẻ pillars Trang chủ, danh sách 12 con số (Numerology); reveal-on-scroll cho 3 bảng tương hợp (Số Chủ Đạo · 12 cung · 12 con giáp). **Có chủ đích BỎ QUA** Hồ sơ/Tương hợp/Lá số vì lưới kết quả nằm trong vùng xuất ảnh PNG (`html-to-image`) — animation sẽ làm hỏng ảnh xuất. **311 test pass** (+1). Build v3.64.
- **v3.63** — **U05 mở rộng hiệu ứng**: (1) `CountUp.jsx` đếm số 0→đích khi cuộn tới (IntersectionObserver, reduced-motion an toàn) — gắn Số Chủ Đạo + bộ số đầy đủ (Numerology) và "Con số ngày" (Today). (2) Stagger lưới khi cuộn: thêm prop `base` cho `Reveal.jsx` + lớp `.stagger-parent` (animation-fill **backwards** nên KHÔNG đè hover transform) — áp cho thư viện 78 lá Tarot, lưới 64 quẻ Kinh Dịch, lưới 12 cung hoàng đạo; mỗi thẻ nhận biến `--i` để vào so le. **310 test pass** (+8 wiring). Build /tmp/b main+single OK; `tam-so-test.html` cập nhật; bump v3.63.
- **v3.62** — **UI/animation + chất lượng diễn giải**: (1) Engine `src/data/tarotSynth.js` (THUẦN, kiểm thử) dệt trải bài Tarot thành đoạn chiêm nghiệm — cán cân xuôi/ngược, sức nặng Ẩn Chính, chất (suit) nổi trội→trọng tâm, lá hoàng gia/Át, khung vị trí đầu→cuối kèm từ khóa thật của lá; thay `spreadSummary` cũ trong `Tarot.jsx`; giữ giọng tham khảo, KHÔNG over-claim. (2) Hiệu ứng: `Reveal.jsx` (IntersectionObserver, tôn trọng prefers-reduced-motion) hiện-dần-khi-cuộn (gắn Home); chuyển trang fade-up `.page-enter` ở `Layout`; ánh quét mặt sau lá Tarot `.cardback-sheen`, dải vàng chạy ở tiêu đề hero `.shimmer-text`, vệt sáng lướt nút chính `.btn-primary`. Thêm CSS ở `index.css` + override reduced-motion. **302 test pass** (+6). ⚠ Gotcha mới xác nhận: Edit/Write TOOL trên ổ D: cắt cụt/đệm NUL khi file PHÌNH → đã chuyển sang `git show HEAD:` + `node fs.writeFileSync` để vá. Build /tmp/b main+single OK; `tam-so-test.html` cập nhật; bump `sw.js`/`package.json` v3.62.
- **v3.61** — **Logo Tam Sở**: emblem "Trăng & sao" (Mẫu 2 người dùng chọn) — vầng trăng khuyết vàng ôm sao ✦ trên đĩa giấy-cổ. Thêm `src/components/Logo.jsx` (SVG inline tái dùng) + `public/logo.svg`; gắn vào nav & footer (thay ký tự ✦ cũ); favicon đổi sang bản trăng-sao nền đậm (data-URI). Bump cache v3.61. **296 test pass**. (v3.60: đồng bộ design-system toàn trang.)
- **(chưa bump version · CHƯA build/test trong sandbox) — Design-system polish (đợt redesign biên tập, học từ concept Stitch hướng "Mực & Vàng")**: thêm thang chữ định danh `display/h1/h2/h3/lead/kicker` + `spacing.section` + bóng `card/lift` vào `tailwind.config.js`; thêm `.rule` (kẻ mảnh biên tập) · `.hover-lift` · `.pull` (trích dẫn in nghiêng) vào `index.css`. **Home.jsx**: lưới 9 công cụ chuyển kiểu biên tập (icon→tiêu đề→mô tả→kẻ mảnh, link LUÔN hiện — sửa lỗi a11y hover-only), "Tử vi hôm nay" thành pull-quote, thẻ quick-start dùng `hover-lift`, hero theo thang chữ. **Chuẩn hoá hero toàn site**: kicker→`text-kicker`, tiêu đề lớn→`text-display`, đoạn dẫn→`text-lead`, footer-kicker→`text-kicker` trên 15 trang + `RelatedLinks` (≈102 trang sâu) + `NotFound`. Grep verify trên file thật: 0 mẫu cũ còn sót, token mới ở 17 file. THUẦN class CSS/Tailwind — không đụng `data/`-logic (không ảnh hưởng test). CÒN LẠI: `npm run build` native (sandbox mount lệch nên không build được) + bump `sw.js`/`package.json` + present trước deploy; phần sâu hơn (section headings, lưới editorial cho trang khác ngoài Home) chưa làm.
- **v3.59** — **Domain tạm**: `SITE_ORIGIN` → `https://tam-so.vercel.app` (bỏ subpath /tam-so vì Vercel chạy ở root). Cập nhật `seo.js`, `sitemap.xml` (216 URL), `index.html` (og/canonical/JSON-LD), `robots.txt`, test (shareUrl mock + SEO expect). Verify: prerender Vercel sinh canonical/og = tam-so.vercel.app; preview khớp. **296 test pass**. Đổi domain lần sau chỉ sửa SITE_ORIGIN + bulk-replace sitemap.
- **v3.58** — **C01**: prerender META tĩnh (`scripts/prerender.mjs`, chạy sau `vite build`) — sinh 211 file `dist/<route>/index.html` với title/description/canonical/og:image + JSON-LD breadcrumb theo từng trang ⇒ crawler "view source" thấy meta đúng. An toàn: KHÔNG sửa `index.html` (file:// ok); GUARD chỉ chạy khi base tuyệt đối `/` (Vercel), skip base `./` (Pages/file://); try/catch ⇒ không bao giờ chặn build. Verify node OK (211 file; the-fool có "Gã Khờ" + og ảnh RWS; index.html giữ nguyên). Còn cần nghiệm thu serving khi Vercel live. **296 test pass** (+4). **HẾT backlog actionable** (chỉ còn nhóm LOW cần backend).
- **v3.57** — **C06 đợt 4 ⇒ C06 HOÀN TẤT**: thêm `src/data/saoKhuyen.js` (lời khuyên 14 chính tinh) render trên Lá Số Tử Vi; xác nhận SAO_CUNG đã dày (168 luận ≥30 ký tự). Trọn C06: 12 cung · 78 lá Tarot · 12 Số · sao×cung. **292 test pass** (+5).
- **v3.56** — **C03**: trang Sinh năm `/sinh-nam` + `/sinh-nam/:year` (1900–2100) tự sinh từ `tinhCanChi` (con giáp, Can Chi, nạp âm/mệnh, tính cách, hợp/khắc tuổi) + `SinhNam.jsx`; meta riêng + sitemap 1950–2025 (+77 URL ⇒ 216 total). **287 test pass** (+6).
- **v3.55** — **A02** og:image động (CardPage dùng ảnh RWS của lá qua `absUrl`; `usePageSeo` nhận `image`) · **M05** Skeleton tải thống nhất (`Skeleton.jsx` + CardImage shimmer khi tải ảnh, object-contain không méo, giảm layout shift) · **C05** rà giọng: test quét >800 luận điểm chặn over-claim ("chắc chắn sẽ/nhất định sẽ/100% đúng"…) + mọi trang hệ có khung "tham khảo/chiêm nghiệm". **281 test pass** (+9).
- **v3.54** — **C04**: trang Hợp tuổi `/hop-tuoi` + 12 trang `/hop-tuoi/:slug` (xếp 12 con giáp theo Tam hợp/Lục hợp/Lục xung/Tứ hành xung dùng `hopTuoiChi`; giọng tham khảo, không định đoạt quan hệ) + `HopTuoi.jsx`; meta+breadcrumb+sitemap (+13 URL ⇒ 139). **272 test pass** (+5).
- **v3.53** — **C06 đợt 3**: làm dày 12 Số Chủ Đạo — thêm `src/data/numerologyDeep.js` (`NUM_DEEP`: tinhYeu/suNghiep/taiChinh/loiKhuyen cho 1–9, 11, 22, 33; giọng tham khảo) + render trên SoChuDaoPage. **267 test pass** (+7). Còn lại C06: đợt 4 — 168 sao×cung.
- **v3.52** — **C06 đợt 2b**: làm dày 56 lá Tarot Ẩn Phụ — thêm `src/data/tarotDeepMinor.js` (ids 22–77: love/work/finance/advice, bám từ khóa sẵn có + chất tố) gộp vào `TAROT_DEEP`; bỏ guard arcana ở CardPage vì id toàn cục 0–77 duy nhất. **TRỌN 78 lá Tarot đã dày**. **260 test pass**. Còn lại C06: 12 Số Chủ Đạo → 168 sao×cung.
- **v3.51** — **C06 đợt 2a**: làm dày 22 lá Tarot Ẩn Chính — thêm `src/data/tarotDeep.js` (`TAROT_DEEP` key id 0–21: love/work/finance/advice; giọng tham khảo, KHÔNG phán cứng) + render trên CardPage (thêm mục 💰 Tài chính; guard `arcana==='major'` để 56 lá Ẩn Phụ không lấy nhầm nội dung major). **259 test pass** (+8). Còn lại: 56 lá Ẩn Phụ → 12 Số Chủ Đạo → 168 sao×cung.
- **v3.50** — **C06 đợt 1**: làm dày 12 cung hoàng đạo — thêm `src/data/zodiacDeep.js` (`ZODIAC_EXTRA`: tomTat/tinhCach/sucKhoe/loiKhuyen; giọng tham khảo, phần sức khỏe có caveat "không thay tư vấn y tế", KHÔNG từ phán tuyệt đối) + render mục mới trên `Zodiac.jsx`. **251 test pass** (+7). Còn lại: 78 lá Tarot → 12 Số Chủ Đạo → 168 sao×cung. **Deploy**: thêm hỗ trợ Vercel (`vercel.json` SPA rewrite + base '/' khi env VERCEL) vì GitHub Pages Actions đang fail do account khoá billing; đã đổi nhánh main→master + sửa `deploy.yml` trigger master.
- **v3.49** — **C02**: "Tử vi hôm nay theo 12 con giáp" — route `/con-giap` + 12 trang `/con-giap/:slug`; thêm `src/data/congiap.js` (CONGIAP_SLUG phân biệt Tý↔Tỵ & Thìn↔Thân, `dailyConGiap` tất định dùng chung engine `dailyReading` tách ra từ zodiac.js, `hopKhacChi` tam/lục hợp + lục xung, `recentYears`) + `src/pages/ConGiap.jsx` (chân dung + tử vi hôm nay + hợp/khắc, giọng tham khảo); meta động + breadcrumb + sitemap (+13 URL ⇒ 126); link trên Home. Phần 12 cung daily đã có sẵn (H04/H05). **244 test pass** (+20). Build /tmp không cần — build native Windows + inline dist→`tam-so-test.html`.
- **v3.46→3.48** — Auto-task tự làm M04/M05 + **A03**: thêm `src/data/related.js` + `RelatedLinks.jsx` ⇒ 102 trang sâu có khối **"Nội dung liên quan"** (liên kết chéo Tarot–Cung–Số, nhãn tham khảo). **216 test pass**.
- **v3.46.1** — FIX bug: `Modal.jsx` nền tối cũ → **kem sáng** (mọi popup quẻ/lá đọc được).
- **v3.41–3.43** — **M02**: 12 trang Số Chủ Đạo `/than-so-hoc/so/:n`. **H02**: trải bài "Tài chính". **M06**: lá số Tử Vi + độ sáng vào báo cáo Hồ sơ. CardPage prev/next.
- **v3.44** — **H07**: trang 404 `NotFound` + catch-all `*`; sửa hồi quy `/tarot/:slug`; slug sai (số/cung/lá) → NotFound.
- **v3.38–3.40** — **H04+H05**: 12 trang cung `/cung-hoang-dao/:slug` + "Tử vi hôm nay" (dailyHoroscope tất định) + ZODIAC_DEEP. **H06**: 78 trang lá `/tarot/:slug`. Sitemap **113 URL**.
- **v3.37** — Trang chủ: lời chào + **streak** + quick-start 3 nhu cầu + widget "Tử vi hôm nay".
- **v3.32–3.36** — Bảng **độ sáng M/V/Đ/B/H** đầy đủ 14 chính tinh (nguồn tracuutuvi, Miếu khớp 100% HOROS) · `doSangSummary` · "Dòng đại hạn cả đời" · mục "Cách đọc lá số" · ghi chú tháng nhuận.
- **(trước v3.32)** 5 hệ đầy đủ (Tarot 78 lá · Thần số học · Tử vi/Can Chi · **Lá số Tử Vi Đẩu Số** · Kinh Dịch 64 quẻ + 64 Thoán/384 hào) + So đôi + Hồ sơ + Cung hoàng đạo + Tương hợp. Đổi sang tông giấy-cổ/kem. Chi tiết: ROADMAP.md.

## Hạ tầng tự động
Scheduled task **tam-so-ai-employee** (hiển thị "ABC XYZ"), 10 phút/lần — **đang PAUSE** (user tắt để tiết kiệm quota gói Max). Tự đọc file → chọn/tự-lên task → sửa (node fs) → test → build /tmp/b → log. Khóa chống chồng `.tamso-run.lock` (epoch nội dung; giữ=date+%s, **nhả=echo 0**, KHÔNG rm). Hiện đã nhả (=0), sạch.

## Phân tích đối thủ
thientue.vn: SEO programmatic quy mô lớn + tử vi ngày + render sẵn HTML; mình hơn về trung thực/chính xác. Bài học → backlog C01–C06.

## Backlog kế (khi bật lại): A01 (SEO index từng hệ) · A02 (og:image động) · M05 (loading skeleton) · C01 (SSG/prerender) · C02 (tử vi ngày 12 con giáp+cung) · C06 (làm dày diễn giải).

## ⚠ DỞ DANG (đợt bị cắt khi user pause) — CHỜ BUILD
- ĐÃ code+wire+test (224 pass) nhưng CHƯA build/bump/present: **M04 Bộ sưu tập** (Collection.jsx + collection.js + route /bo-suu-tap + nút lưu Tarot/Kinh Dịch + badge nav) & **A01 SEO từng trang** (useSeo.js/SeoTag.jsx + usePageSeo gắn 8 trang).
- File mới (untracked): src/pages/Collection.jsx, src/data/collection.js, src/components/useSeo.js, src/components/SeoTag.jsx, src/data/seo.js.
- VIỆC CÒN LẠI: chỉ cần BUILD /tmp/b (verify dist IDENTICAL + NUL=0) + bump sw/package + present. CHƯA build-verify JSX → build 1 lần trước khi deploy. ĐỪNG làm lại M04/A01 (đã xong).
